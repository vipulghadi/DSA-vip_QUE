
    // using extra space
    // vector<int>nz;
    // 	   vector<int>z;
    // 	   for(int i=0;i<n;i++)
    // 	   {
    // 	       if(arr[i]!=0)
    // 	       {
    // 	           nz.push_back(arr[i]);
    // 	       }
    // 	       else{
    // 	           z.push_back(arr[i]);
    // 	       }
    // 	   }
    // 	   int k=0;
    // 	   int l=0;
    // 	   for(int i=0;i<n;i++)
    // 	   {
    // 	       if(i<nz.size())
    // 	       {
    // 	           arr[i]=nz[k++];
    // 	       }
    // 	       else
    // 	       {
    // 	           arr[i]=z[l++];
    // 	       }
    //     	   }

    //--------------------------------------------------------------------

    // int k;
    // 	   for(int i=0;i<n;i++)
    // 	   {
    // 	      if(arr[i]!=0)
    // 	      {
    // 	         k=i;

    // 	         while(arr[k-1]=0 && k>0)
    // 	         {
    // 	             swap(arr[k],arr[k-1]);
    // 	             k--;
    // 	         }

    // 	      }

    // 	   }
